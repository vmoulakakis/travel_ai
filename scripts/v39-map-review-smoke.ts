import { readFileSync } from "node:fs";
const read=(p:string)=>readFileSync(p,"utf8"),failures:string[]=[];const expect=(ok:boolean,msg:string)=>{if(!ok)failures.push(msg)};
const page=read("app/escape/[slug]/page.tsx"),map=read("components/v39-destination-map-workspace.tsx"),stay=read("app/escape/[slug]/stay/[offerId]/page.tsx"),v50=stay.includes("V50StayFunnel"),v40=stay.includes("V40StayWorkspace"),activeStay=read(v50?"components/v50-stay-funnel.tsx":v40?"components/v40-stay-workspace.tsx":"components/v39-stay-review-hero.tsx"),reviews=read("lib/data/stay-review-intelligence-v39.ts"),api=read("app/api/escape/stay-reviews/route.ts"),feedback=read("components/v39-stay-feedback.tsx");
expect(page.includes("V39DestinationMapWorkspace")&&page.includes("loadV8StayOffers(slug,start,end,60)"),"destination route must open the real-inventory map with deep retrieval");
expect(map.includes("slice(0,10)")&&map.includes("leaflet")&&map.includes("flyTo")&&map.includes("Explore this stay"),"map must expose top-10 cards, pin/card zoom and internal stay CTA");
expect(map.includes("/api/escape/stay-reviews")&&map.includes("ratings.find")&&map.includes("AI Guest Signal"),"map must request and render review intelligence before stay selection");
expect((stay.includes("selected=loaded.find")||stay.includes("directSelected")||stay.includes("loadV8StayOfferById"))&&stay.includes("V39StayFeedback")&&(stay.includes("V50StayFunnel")||stay.includes("V40StayWorkspace")||stay.includes("V39StayReviewHero")),"selected stay route must pin the exact offer, keep post-trip feedback and own an internal stay experience");
expect((v50||v40)?(activeStay.includes("/api/escape/stay-reviews")&&activeStay.includes("/api/escape/stay-local")&&(activeStay.includes("No external links")||activeStay.includes("μοναδικό εξωτερικό εμπορικό link"))):(stay.includes('a[href^="http"]:not([rel~="sponsored"])')),"selected stay experience must expose provider evidence without provider outbound links");
expect(reviews.includes("Google Places")&&reviews.includes("Tripadvisor")&&reviews.includes("Foursquare")&&reviews.includes("AI Guest Signal"),"review intelligence must support multi-source ratings plus first-party signal");
expect(reviews.includes("Never invent facts")&&reviews.includes("AI summarizes available evidence and never invents reviews"),"AI review synthesis must explicitly forbid fabricated reviews");
expect(api.includes("x-travel-review-engine")&&api.includes("v39-truthful-multi-source"),"review API must expose truth provenance");
expect(feedback.includes('subjectKind:"stay"')&&feedback.includes("tripEnd<=new Date")&&(feedback.includes("at least 3")||feedback.includes("τουλάχιστον 3")),"first-party stay feedback must be post-trip and explain the 3-experience sample gate");
expect((v50||v40)?(activeStay.includes("Disclosure")&&activeStay.includes("itinerary")&&activeStay.includes("/api/guide/email")):(activeStay.includes("Disclosure")&&activeStay.includes("verified ratings")&&activeStay.includes("#escape-builder")),"stay experience must show disclosure before commerce and retain itinerary/email handoff");
if(failures.length){console.error(`V39/V40 map-review smoke failed (${failures.length})`);for(const x of failures)console.error(`- ${x}`);process.exit(1)}console.log(`V39 map/review truth contracts passed with ${v50?"V50 stay funnel":v40?"V40 stay workspace":"V39 landing"}`);
