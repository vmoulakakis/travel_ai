# V20 scope freeze

V20 is intentionally a production-truth release, not another semantic-ranking rewrite.

In scope: availability truth, evidence-coverage observability, production commit verification, inventory RPC least privilege, permanent regression coverage.

Out of scope: changing semantic ranking thresholds, weakening V19 gates, adding destinations for diversity, inventing persistent evidence, changing affiliate economics, or claiming room availability when the provider feed does not explicitly supply stock state.
