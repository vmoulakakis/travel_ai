name: V19 semantic fix validation
on:
  push:
    branches: [agent/v19-10000-match-audit]
    paths: [.github/workflows/v19-semantic-fix.yml]
permissions:
  contents: write
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v19-10000-match-audit
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: python3 scripts/v19-refine-semantic.py
      - run: npm install
      - run: npm run typecheck
      - run: npm run test:v18:semantic
      - run: npm run test:v19:match10000
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add lib/ai/semantic-fallback-v19.ts scripts/v19-10000-semantic-match-audit.ts
          git diff --cached --check
          git commit -m "Refine mixed, priority and natural-language semantics"
          git push
