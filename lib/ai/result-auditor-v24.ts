import { geographyConstraint,matchesGeographyConstraint } from "@/lib/decision/geography-constraint";
import { diversifyV24 } from "@/lib/decision/v24-matcher";
import type { V8Ranked } from "@/lib/decision/v8-matcher";
import type { TripRequest } from "@/lib/validation/trip";
import { researchIntent,satisfiesResearchNeed } from "@/lib/decision/research-intent-v13";
import type { DestinationEvidenceBundle } from "@/lib/decision/types";
import { createLLMRequestBudgetV16,generateJsonWithRoutingV16,type LLMRequestBudgetV16 } from "@/lib/ai/model-router-v9";

export type V24AuditIssue={slug:string;code:"HARD_CONSTRAINT"|"MUST_HAVE"|"LOW_EVIDENCE"|"RESEARCH_EVIDENCE";detail:string};
export type V24AuditResult={passed:boolean;confidence:"HIGH"|"MEDIUM"|"LOW";issues:V24AuditIssue[];attempts:number;checkedBy:"deterministic"|"deterministic+llm"};
function mustHave(request:TripRequest,item:V8Ranked){if(request.mustHave==="sea")return item.destination.tags.includes("beach");if(request.mustHave==="nature")return item.destination.tags.includes("nature");if(request.mustHave==="culture")return item.destination.tags.includes("culture");if(request.mustHave==="nightlife")return item.destination.tags.includes("nightlife")||item.destination.tags.includes("city");return true}
function deterministicAudit(request:TripRequest,items:V8Ranked[],catalog:V8Ranked["destination"][],evidence?:Map<string,DestinationEvidenceBundle>):V24AuditIssue[]{const constraint=geographyConstraint(request,catalog),research=researchIntent(request),issues:V24AuditIssue[]=[];for(const item of items){if(!matchesGeographyConstraint(item.destination,constraint))issues.push({slug:item.destination.slug,code:"HARD_CONSTRAINT",detail:"Does not satisfy the explicit geography constraint"});if(!mustHave(request,item))issues.push({slug:item.destination.slug,code:"MUST_HAVE",detail:"Does not satisfy the selected must-have"});if(item.breakdown.season<35||item.breakdown.routeConfidence<25)issues.push({slug:item.destination.slug,code:"LOW_EVIDENCE",detail:"Season or route evidence is below the safe floor"});for(const need of research.needs){const bundle=evidence?.get(item.destination.slug);if(!bundle||!satisfiesResearchNeed(bundle,need))issues.push({slug:item.destination.slug,code:"RESEARCH_EVIDENCE",detail:`Missing verified ${need} evidence for the requested dates`})}}return issues}
async function independentAudit(request:TripRequest,items:V8Ranked[],budget:LLMRequestBudgetV16){const candidates=items.map(item=>({slug:item.destination.slug,locality:item.destination.localityLabel??item.destination.nameEn,parent:item.destination.canonicalParentSlug??null,tags:item.destination.tags,evidence:item.breakdown})),routed=await generateJsonWithRoutingV16<{reject_slugs:string[]}>({context:{task:"verification",text:request.tripText,deterministicConfidence:.82,hardConstraintRisk:false,contradictorySignals:false},budget,system:"You are an independent result auditor, never a recommender. Audit only supplied constraints and numeric evidence. Never invent facts.",prompt:`Return JSON only {"reject_slugs":[]}. Reject only clear contradictions. Request=${JSON.stringify({freeText:request.tripText,mustHave:request.mustHave,avoid:request.avoid,distance:request.distancePreference,dates:[request.startDate,request.endDate]})}; Candidates=${JSON.stringify(candidates)}`,validate(value){const reject_slugs=Array.isArray(value.reject_slugs)?value.reject_slugs.filter((slug):slug is string=>typeof slug==="string"&&items.some(item=>item.destination.slug===slug)).slice(0,3):[];return{reject_slugs}}});return new Set(routed?.value.reject_slugs??[])}
export async function auditAndRepairV24(request:TripRequest,initial:V8Ranked[],pool:V8Ranked[],limit=12,evidence?:Map<string,DestinationEvidenceBundle>,budget:LLMRequestBudgetV16=createLLMRequestBudgetV16()):Promise<{items:V8Ranked[];audit:V24AuditResult}>{const catalog=pool.map(item=>item.destination);let items=initial.slice(0,limit),lastIssues:V24AuditIssue[]=[],usedLLM=false;for(let attempt=1;attempt<=3;attempt++){const deterministic=deterministicAudit(request,items,catalog,evidence),localReject=deterministic.length?new Set<string>():await independentAudit(request,items,budget);if(localReject.size)usedLLM=true;const issues=[...deterministic,...[...localReject].map(slug=>({slug,code:"LOW_EVIDENCE" as const,detail:"Independent audit rejected an evidence inconsistency"}))];if(!issues.length)return{items,audit:{passed:true,confidence:"HIGH",issues:[],attempts:attempt,checkedBy:usedLLM?"deterministic+llm":"deterministic"}};lastIssues=issues;const rejected=new Set(issues.map(x=>x.slug)),survivors=pool.filter(item=>!rejected.has(item.destination.slug)&&!deterministicAudit(request,[item],catalog,evidence).length);items=diversifyV24(survivors,limit);if(!items.length)break}return{items:[],audit:{passed:false,confidence:"LOW",issues:lastIssues,attempts:3,checkedBy:usedLLM?"deterministic+llm":"deterministic"}}}
